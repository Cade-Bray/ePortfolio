# Application configuration properties example. Replace values as needed in production.
# Do NOT commit sensitive information once you've filled it out.
# This file is for demonstration purposes only.
api.rootAddress=http://localhost:3000/api
api.deviceId=test-device-001

# Secret should be set via environment variable for security reasons. Do not hardcode it here.
# Linux environments would be in /etc/environment
api.deviceSecret=${DEVICE_SECRET:}